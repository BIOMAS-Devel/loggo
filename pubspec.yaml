name: loggo
description: Annotation-driven logging package for Dart and Flutter with rules, targets, and source generation support.
version: 0.0.1
publish_to: https://pub.dev

environment:
  sdk: ^3.9.0

dependencies:
  analyzer: ^8.1.1
  build: ^4.0.0
  intl: ^0.20.2
  source_gen: ^4.0.1

dev_dependencies:
  build_runner: ^2.8.0
  lints: ^6.0.0
  test: ^1.26.0
